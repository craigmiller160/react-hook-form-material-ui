import React from 'react';

describe('TextField', () => {
    it('accepts input for type text', () => {
        throw new Error();
    });

    it('accepts input for type number', () => {
        throw new Error();
    })

    it('displays error message for validation rules', () => {
        throw new Error();
    });

    it('transforms input', () => {
        throw new Error();
    });

    it('is disabled', () => {
        throw new Error();
    });
});